CREATE TABLE STUDENT_DETAILS (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    S_ID INT UNIQUE ,
    NAME VARCHAR(20) NOT NULL,
    MAJOR VARCHAR(35) NOT NULL
);

ALTER TABLE STUDENT_DETAILS AUTO_INCREMENT = 1000;

DROP TABLE STUDENT_DETAILS;

SELECT * FROM STUDENT_DETAILS WHERE S_ID=?;

INSERT INTO STUDENT_DETAILS(S_ID,NAME,MAJOR)VALUES(?,?,?);

DELETE FROM STUDENT_DETAILS WHERE S_ID=?

UPDATE STUDENT_DETAILS SET NAME=?, MAJOR=? WHERE S_ID=?;